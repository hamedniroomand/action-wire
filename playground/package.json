{
  "name": "playground",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json && tsc -p test/tsconfig.json && tsc -p compatibility/tsconfig.json && tsc -p server/tsconfig.json"
  },
  "dependencies": {
    "action-wire": "workspace:*"
  }
}
